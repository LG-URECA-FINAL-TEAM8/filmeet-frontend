import styled from "styled-components";

export const StarRatingPageWrapper = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  background-color: ${(props) => props.theme.color.mainColor};
`;
